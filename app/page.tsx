'use client';
import { useState } from 'react';
import { Tabs,TabsList,TabsTrigger } from '@/components/ui/tabs';
import { Sheet,SheetContent,SheetTitle,SheetDescription } from '@/components/ui/sheet';
import { Dialog,DialogContent,DialogTitle,DialogDescription } from '@/components/ui/dialog';
import { Switch } from '@/components/ui/switch';
import { Table,TableHeader,TableHead,TableBody,TableRow,TableCell } from '@/components/ui/table';
import { Sparkles,ArrowUpRight,CheckCircle2,TriangleAlert,ChevronRight,ChevronLeft,Download,Plus,Upload,FileText,Link2,MessageSquare,ArrowRight,Search,Send,Check,Flag } from 'lucide-react';
const subjects=['国語','数学','英語','理科','社会'];
const areas:Record<string,string[]>= {'数学':['数と式','関数','図形','資料の活用'],'国語':['漢字・語句','説明的文章','文学的文章','古典'],'英語':['語彙・文法','読解','英作文','聞き取り'],'理科':['物理','化学','生物','地学'],'社会':['地理','歴史','公民','資料読解']};
const stems:Record<string,string[]>={数学:['方程式 3x − 7 = 8 を解きなさい。','関数 y = ax² について、xの値が2から4まで増加するときの変化の割合は6である。aの値を求めなさい。','直角三角形の2辺の長さが3cm、4cmである。残りの辺の長さを求めなさい。','次の度数分布表をもとに、最頻値を求めなさい。'],国語:['「推敲」の読みを書きなさい。','筆者が述べる「豊かさ」とは何か。本文の内容に基づき説明しなさい。','傍線部の人物の心情を、理由とともに説明しなさい。','「いとをかし」の現代語訳を書きなさい。'],英語:['She ( ) to school every day. 空欄に適する語を書きなさい。','本文で筆者が提案していることを日本語で説明しなさい。','あなたの好きな季節について、その理由を英語で書きなさい。','音声の内容に合う選択肢を選びなさい。'],理科:['電圧6V、抵抗3Ωのとき、流れる電流を求めなさい。','反応前後で全体の質量はどのように変化するか。','植物が光合成を行う条件を説明しなさい。','図から前線が通過した時刻を読み取りなさい。'],社会:['地図中の地域で盛んな産業を答えなさい。','資料中の政策が実施された目的を説明しなさい。','国会と内閣の関係を説明しなさい。','グラフから読み取れる人口の変化を説明しなさい。']};
type Test={id:string;name:string;start:string;end:string;demo:boolean};
type Series={id:string;name:string;tests:Test[]};
const initial:Series[]=[{id:'s1',name:'中学3年生向け模擬試験',tests:[{id:'t2',name:'第2回 模擬試験',start:'2026-08-24',end:'2026-09-06',demo:true},{id:'t1',name:'第1回 模擬試験',start:'2026-06-15',end:'2026-06-28',demo:true}]},{id:'s2',name:'中学3年生 単元テスト',tests:[]},{id:'s3',name:'中学2年生 単元テスト',tests:[]}];
function Curve({item=false,b=.2,a=1.2}:{item?:boolean;b?:number;a?:number}){let points=Array.from({length:81},(_,i)=>{const x=-3+i*6/80;const y=item?1/(1+Math.exp(-a*(x-b))):Math.exp(-Math.pow(x-.15,2)/1.9);return `${45+i*5},${175-y*140}`}).join(' ');return <svg viewBox="0 0 470 222" role="img" aria-label={item?'項目特性曲線のデモ':'テスト情報曲線のデモ'} className="curve">{[0,1,2,3,4].map(i=><g key={i}><line x1="45" y1={35+i*35} x2="445" y2={35+i*35} stroke="#e6edf1"/><text x="12" y={40+i*35} fontSize="11" fill="#6b8090">{item?(1-i*.25).toFixed(2):20-i*5}</text></g>)}<polyline points={points} fill="none" stroke="#087f83" strokeWidth="3.5"/>{[-3,-2,-1,0,1,2,3].map((x,i)=><text key={x} x={45+i*400/6} y="196" textAnchor="middle" fontSize="12" fill="#6b8090">{x}</text>)}<text x="240" y="219" textAnchor="middle" fontSize="12" fill="#6b8090">能力値 θ（デモ尺度）</text></svg>}
export default function Home(){
const [series,setSeries]=useState(initial),[sid,setSid]=useState('s1'),[tid,setTid]=useState('t2'),[view,setView]=useState('test'),[subject,setSubject]=useState('数学'),[tab,setTab]=useState('summary'),[selected,setSelected]=useState<number|null>(null),[filter,setFilter]=useState('すべて'),[search,setSearch]=useState(''),[metric,setMetric]=useState('配点'),[dimension,setDimension]=useState('領域');
const [marks,setMarks]=useState<Record<string,boolean>>({}),[notes,setNotes]=useState<Record<string,string>>({}),[draft,setDraft]=useState(''),[keys,setKeys]=useState<Record<string,boolean>>({}),[excluded,setExcluded]=useState<Record<string,boolean>>({}),[notice,setNotice]=useState(''),[modal,setModal]=useState(''),[name,setName]=useState(''),[start,setStart]=useState('2026-09-01'),[end,setEnd]=useState('2026-09-14');
const [ai,setAi]=useState(false),[question,setQuestion]=useState(''),[reply,setReply]=useState(''),[asked,setAsked]=useState(''),[answerFilter,setAnswerFilter]=useState('すべて');
const [files,setFiles]=useState<Record<string,string>>({}),[pending,setPending]=useState<{kind:string;name:string}|null>(null),[revision,setRevision]=useState<Record<string,number>>({}),[ocr,setOcr]=useState<Record<string,string>>({});
const current=series.find(s=>s.id===sid)!,test=current.tests.find(t=>t.id===tid),context=`${sid}/${tid}/${subject}`,labels=areas[subject],rev=revision[context]||0;
const ready=!!test?.demo||!!files[context+'/正誤CSV'];
const items=Array.from({length:24},(_,i)=>{const area=i<6?0:i<16?1:i<21?2:3;return {id:i,no:`${Math.floor(i/4)+1}（${i%4+1}）`,area:labels[area],unit:[labels[area]+'の基本',labels[area]+'の応用'][i%2],point:[0,1,6,7].includes(i)?5:4,rate:Math.max(12,93-i*3),b:Math.round((-1.8+i*.16)*100)/100,a:[7,13,20].includes(i)?.38:1.2,warning:[7,13,20].includes(i),stem:stems[subject][area]}});
const item=selected===null?null:items[selected];const key=(i:number)=>`${context}/${i}`;
function tell(s:string){setNotice(s);setTimeout(()=>setNotice(''),4500)}
function openItem(i:number){setSelected(i);setDraft(notes[key(i)]||'');setAnswerFilter('すべて');setReply('');setAsked('')}
function navigate(t:string){setTab(t);setSelected(null)}
function resetScope(){setSelected(null);setAi(false);setReply('');setAsked('');setFilter('すべて');setSearch('')}
function ask(q:string){if(!q.trim())return;setAsked(q);setQuestion('');setReply(item?`【確認できる事実】\n設問 ${item.no} の正答率は${item.rate}%、難易度は${item.b}です。${item.warning?'識別力は0.38で、要確認候補です。':'識別力は1.20です。'}\n\n【考えられる問題点】\n${subject==='数学'&&selected===20?'「2辺」のどちらが斜辺か指定されていないため、条件の読み方が分かれる可能性があります。':'求める回答の条件や範囲が、受験者に十分伝わっているか確認してください。'}\n\n【改善案】\n${subject==='数学'&&selected===20?'「直角をはさむ2辺の長さが3cm、4cmの直角三角形について、斜辺の長さを求めなさい」と条件を明示します。':'解答形式と必要な条件を問題文に明記し、問いを一つの学習項目に絞る案が考えられます。'}\n\n【追加の確認】\n実際の誤答と採点基準を照合してください。改訂後の難易度は再実施で確認が必要です。`:`【良い点】\n標準的な難易度を中心に、基礎から応用までを含む構成です。\n\n【確認したい点】\n${labels[1]}が10問・42点に集中しています。${labels[3]}は3問・12点です。目標構成は未設定のため、この差だけで不適切とは判断しません。\n\n【改善案】\n試験の目的と出題範囲を確認し、${labels[1]}の重複した学習項目を見直して、${labels[3]}の問題を追加する案を検討してください。\n\n【確認の優先順位】\nまず要確認の3問を確認し、次に問題セット全体の配点構成を見直してください。`);setAi(true)}
const filtered=items.filter(i=>(filter==='すべて'||filter==='要確認'&&i.warning||filter==='要改善'&&marks[key(i.id)]||filter===i.area||filter==='高難易度'&&i.b>=1||filter==='標準'&&i.b>=-.5&&i.b<.5)&&(!search||`${i.no} ${i.stem} ${i.area}`.includes(search)));
function exportReport(){const content=`# Testscope 品質レビュー（評価用デモ）\n${current.name} / ${test?.name} / ${subject}\n実施期間：${test?.start}〜${test?.end}\n\nIRT・AI・OCRは模擬値であり実分析ではありません。\n\n## 要確認問題\n${items.filter(i=>i.warning).map(i=>`- ${i.no}: 正答率${i.rate}%、難易度${i.b}、識別力${i.a}`).join('\n')}\n\n## 要改善・コメント\n${items.filter(i=>marks[key(i.id)]||notes[key(i.id)]).map(i=>`- ${i.no} ${marks[key(i.id)]?'要改善':''}\n  ${notes[key(i.id)]||''}`).join('\n')}`;const u=URL.createObjectURL(new Blob([content],{type:'text/markdown;charset=utf-8'}));const a=document.createElement('a');a.href=u;a.download=`Testscope_${subject}_評価レポート.md`;a.click();URL.revokeObjectURL(u);tell('評価レポートを出力しました')}
return <><header className="top"><button className="brand" onClick={()=>{setView('series');resetScope()}}>▥ <b>Testscope</b></button><span>問題品質レビュー</span><span className="user">テスト実施者</span></header><div className="demo">評価用モック · 架空データ / IRT・OCR・AIはデモ · 操作内容はこのページを再読込するとリセットされます</div><main className="workspace"><div className="crumb"><button onClick={()=>{setView('series');resetScope()}}>シリーズ</button><ChevronRight size={14}/><button onClick={()=>{setView('tests');resetScope()}}>{current.name}</button>{view==='test'&&<><ChevronRight size={14}/>{test?.name}</>}</div>
{view==='series'?<><div className="heading"><div><p className="eyebrow">TEST SERIES</p><h1>テストシリーズ</h1><p className="muted">継続するテストを、ひとつのシリーズに。</p></div><button className="primary" onClick={()=>{setName('');setModal('series')}}><Plus size={17}/>シリーズを登録</button></div><div className="seriesgrid">{series.map(s=><button className="card seriescard" key={s.id} onClick={()=>{setSid(s.id);setView('tests')}}><span className="seriesicon">▥</span><h2>{s.name}</h2><p className="muted">{s.tests.length}回のテスト · 5教科</p><ArrowUpRight size={20}/></button>)}</div></>:view==='tests'?<><div className="heading"><div><p className="eyebrow">SERIES OVERVIEW</p><h1>{current.name}</h1></div><button className="primary" onClick={()=>{setName('');setModal('test')}}><Plus size={17}/>テストを登録</button></div>{current.tests.length===0?<div className="card empty"><h2>まだテストがありません</h2><p>実施回と期間を登録すると、5教科のデータを登録できます。</p></div>:current.tests.map(t=><div className="card testrow" key={t.id}><div><h2>{t.name}</h2><p className="muted">{t.start} – {t.end}</p></div><span className="badge">{t.demo?'5教科 分析済み（デモ）':'データ未登録'}</span><button className="secondary" onClick={()=>{setTid(t.id);setView('test');navigate(t.demo?'summary':'data');resetScope()}}>テストを開く<ArrowRight size={16}/></button></div>)}</>:<><div className="heading"><div><p className="eyebrow">TEST QUALITY REVIEW</p><h1>{test?.name}</h1><button className="muted period" onClick={()=>{setName(test?.name||'');setStart(test?.start||'');setEnd(test?.end||'');setModal('period')}}>{test?.start} – {test?.end} <span>編集</span></button></div><div className="actions"><button className="secondary" onClick={()=>window.print()}>印刷 / PDF</button><button className="secondary" onClick={exportReport}><Download size={16}/>レポート</button><button className="primary" disabled={!ready} onClick={()=>{setAsked('');setReply('');setAi(true)}}><Sparkles size={18}/>{item?'AIに改善案を聞く':'AIに問題セットの改善案を聞く'}</button></div></div><Tabs value={subject} onValueChange={v=>{setSubject(String(v));resetScope()}}><TabsList className="subjects">{subjects.map(s=><TabsTrigger key={s} value={s}>{s}</TabsTrigger>)}</TabsList></Tabs><Tabs value={tab} onValueChange={v=>navigate(String(v))}><TabsList variant="line" className="viewtabs"><TabsTrigger value="summary">品質サマリー</TabsTrigger><TabsTrigger value="items">問題一覧 <span className="count">24</span></TabsTrigger><TabsTrigger value="data">データ・資料</TabsTrigger></TabsList></Tabs>
{!ready&&tab!=='data'?<div className="card empty"><Upload/><h2>まず、正誤データを登録しましょう</h2><p>このモックでは、登録後にサンプル分析へ進めます。</p><button className="primary" onClick={()=>navigate('data')}>データを登録</button></div>:tab==='summary'?<><div className="sectionhead"><h2>問題セットの品質</h2><span className="muted">{subject} · 24問 / 100点 · デモ版 {rev+1}</span></div><div className="insight"><div className="insighttitle"><Sparkles size={22}/><h2>基礎を測る構成は良好。{labels[1]}への集中に、見直しの余地。</h2></div><p>標準的な難易度を中心に構成されています。一方、{labels[1]}に配点の42%が集中。{labels[3]}は12%にとどまります。試験の目的に合った構成かを確認しましょう。</p><div className="insightfoot"><span><CheckCircle2 size={17}/> 基礎問題の識別は安定</span><button onClick={()=>{setFilter('要確認');navigate('items')}}><TriangleAlert size={17}/> 3問を確認する <ArrowRight size={15}/></button><button onClick={()=>ask('問題セットの改善案を教えて')}><Sparkles size={16}/> AIで詳しく検討</button></div></div><div className="statgrid">{[['受験者数','480','人'],['平均点','58.4','/ 100'],['要確認問題','3','/ 24問'],['要改善マーク',String(items.filter(i=>marks[key(i.id)]).length),'問']].map(([l,n,u])=><div className="stat" key={l}><p>{l}</p><b>{n}</b><span>{u}</span></div>)}</div><div className="grid2"><section className="card"><div className="sectionhead compact"><h2>出題内容の構成</h2><div className="seg">{['配点','問題数'].map(m=><button className={metric===m?'on':''} onClick={()=>setMetric(m)} key={m}>{m}</button>)}</div></div><div className="chips">{['領域','単元','学習項目'].map(d=><button key={d} className={dimension===d?'on':''} onClick={()=>setDimension(d)}>{d}</button>)}</div>{labels.map((s,i)=><button className="barrow barbutton" key={s} onClick={()=>{setFilter(s);navigate('items')}}><span>{s}{dimension!=='領域'?(dimension==='単元'?'の基本':'の理解'):''}</span><div className="track"><div style={{width:(metric==='配点'?[26,42,20,12][i]:[25,41.7,20.8,12.5][i])+'%'}}/></div><b>{metric==='配点'?[26,42,20,12][i]+'%':[6,10,5,3][i]+'問'}</b></button>)}<p className="footnote">目標構成は未設定です。偏りだけで良否を判断しません。<br/>分類切替は集計表示のデモです。</p></section><section className="card"><h2>難易度の構成</h2><p className="muted">IRT難易度 · 24問のデモ推定値</p><div className="hist">{[6,3,6,3,6].map((n,i)=><button key={i} onClick={()=>{setFilter(i===4?'高難易度':i===2?'標準':'すべて');navigate('items')}}><b>{n}問</b><div style={{height:n*17}}/><span>{['−1以下','−1〜−0.5','−0.5〜0.5','0.5〜1','1以上'][i]}</span></button>)}</div><p className="footnote">低い ← 難易度 b → 高い / モデル：2PL（例示）</p></section></div><div className="grid2"><section className="card"><h2>問題セットの測定特性</h2><p className="muted">テスト情報曲線（説明用の模式図）</p><Curve/><p className="footnote">標準層付近の情報量が高い構成例です。</p></section><section className="card"><div className="sectionhead compact"><h2>優先して確認したい問題</h2><span className="badge amber">3問</span></div>{items.filter(i=>i.warning).map(i=><button className="reviewrow" key={i.id} onClick={()=>{setTab('items');openItem(i.id)}}><span className="itemnum">{i.no}</span><div><b>{i.area} · {i.id===20?'条件の明確さを確認':'識別力を確認'}</b><p className="muted">識別力 {i.a.toFixed(2)} / 正答率 {i.rate}%</p></div><ChevronRight size={18}/></button>)}<button className="textlink" onClick={()=>{setFilter('すべて');navigate('items')}}>すべての問題を見る <ArrowRight size={15}/></button></section></div></>:tab==='items'&&!item?<><div className="sectionhead"><div><h2>問題一覧</h2><p className="muted">システムの「要確認」と、ご自身の「要改善」を分けて確認</p></div><button className="secondary" onClick={()=>setModal('keys')}><Link2 size={16}/>共通問題・IRTキー</button></div><div className="toolbar"><div className="chips">{['すべて','要確認','要改善',...labels,'高難易度'].map(f=><button key={f} className={filter===f?'on':''} onClick={()=>setFilter(f)}>{f}</button>)}</div><label className="search"><Search size={16}/><input aria-label="問題を検索" placeholder="設問番号・問題文を検索" value={search} onChange={e=>setSearch(e.target.value)}/></label></div><div className="card tablecard"><Table><TableHeader><TableRow>{['設問','領域 / 問題','配点','正答率','難易度 b','識別力 a','状態',''].map((s,i)=><TableHead key={i}>{s}</TableHead>)}</TableRow></TableHeader><TableBody>{filtered.map(i=><TableRow key={i.id}><TableCell><button className="textlink" onClick={()=>openItem(i.id)}>{i.no}</button></TableCell><TableCell><b>{i.area}</b><p className="truncate">{i.stem}</p></TableCell><TableCell>{i.point}</TableCell><TableCell>{i.rate}%</TableCell><TableCell>{i.b.toFixed(2)}</TableCell><TableCell>{i.a.toFixed(2)}</TableCell><TableCell>{i.warning&&<span className="badge amber">要確認</span>} {marks[key(i.id)]&&<span className="badge">要改善</span>}{!i.warning&&!marks[key(i.id)]&&<span className="muted">—</span>}</TableCell><TableCell><button aria-label={`${i.no}の詳細`} onClick={()=>openItem(i.id)}><ChevronRight size={18}/></button></TableCell></TableRow>)}</TableBody></Table>{!filtered.length&&<p className="empty">該当する問題はありません。</p>}</div><p className="footnote">{filtered.length} / 24問 · IRTの値と判定基準はサンプルです。</p></>:tab==='items'&&item?<><div className="sectionhead"><button className="textlink" onClick={()=>setSelected(null)}><ChevronLeft size={16}/>問題一覧へ戻る</button><div className="actions"><button className="secondary" disabled={item.id===0} onClick={()=>openItem(item.id-1)}>前の問題</button><button className="secondary" disabled={item.id===23} onClick={()=>openItem(item.id+1)}>次の問題</button></div></div><div className="detailhead"><div><span className="eyebrow">ITEM REVIEW</span><h2>設問 {item.no} <span className="muted">{item.area} · {item.point}点</span></h2></div><label className="mark"><Flag size={17}/>要改善<Switch checked={!!marks[key(item.id)]} onCheckedChange={v=>{setMarks({...marks,[key(item.id)]:v});tell(v?'要改善マークを付けました':'マークを解除しました')}} aria-label="要改善マーク"/></label></div><div className="grid2"><section className="card"><div className="sectionhead compact"><h2>問題と正解</h2><span className="badge">OCR確認済みの例</span></div><div className="paper"><div className="paperlabel">問題用紙 · 抜粋見本 / {subject}</div><h3>{item.no}</h3><p>{ocr[key(item.id)]||item.stem}</p><div className="answerline">解答欄　________________</div></div><div className="solution"><b>正解・模範解答（例）</b><p>{subject==='数学'?item.id<6?'x = 5':item.id<16?'a = 1':item.id<21?'5 cm（直角をはさむ2辺の場合）':'最も度数が大きい階級の値':'問いの条件を満たし、根拠を明確にした回答。'}</p></div><button className="textlink" onClick={()=>{setDraft(ocr[key(item.id)]||item.stem);setModal('ocr')}}>OCRテキストを確認・修正 <ArrowUpRight size={15}/></button><p className="footnote">原本・答案は画面評価のための見本です。</p></section><section className="card"><h2>IRTによる問題特性</h2><p className="muted">項目特性曲線 · 縦軸：正答確率</p><Curve item b={item.b} a={item.a}/><div className="ministats"><span>難易度 <b>{item.b.toFixed(2)}</b></span><span>識別力 <b>{item.a.toFixed(2)}</b></span><span>正答率 <b>{item.rate}%</b></span></div><div className={item.warning?'callout amberbox':'callout'}><b>{item.warning?'要確認：識別力と問題の条件':'明確な警告はありません（デモ）'}</b><p>{item.warning?'曲線の立ち上がりが緩やかな例です。問題の条件や採点基準、実際の解答を確認してください。':'問題内容と、意図した学習項目が一致しているか確認できます。'}</p></div><button className="primary full" onClick={()=>ask('この問題の改善案を教えて')}><Sparkles size={17}/>この問題の改善案をAIに聞く</button></section></div><section className="card below"><div className="sectionhead compact"><div><h2>この問題への生徒の解答</h2><p className="muted">OCR解答・原本の該当箇所の見本 / 6人分のみ</p></div><div className="chips">{['すべて','正解','不正解','未回答'].map(f=><button key={f} className={answerFilter===f?'on':''} onClick={()=>setAnswerFilter(f)}>{f}</button>)}</div></div><div className="answers">{['正解','不正解','不正解','正解','未回答','不正解'].map((s,i)=>({s,i})).filter(x=>answerFilter==='すべて'||x.s===answerFilter).map(({s,i})=><div className="answercard" key={i}><div className="between"><b>S{String(1001+i)}</b><span className={s==='正解'?'badge':'badge amber'}>{s}</span></div><div className="handwriting">{s==='未回答'?'（空欄）':subject==='数学'?(s==='正解'?(item.id<6?'x = 5':item.id<16?'a = 1':'5 cm'):['3 + 4 = 7','√(4² − 3²) = √7','a = 2'][i%3]):s==='正解'?'条件を根拠に説明した回答':'根拠が省略された回答'}</div><p className="muted">OCR：{s==='未回答'?'空欄を確認':s==='正解'?'解答を読み取り':'解答の根拠を確認'} {i===2?' / 読取要確認':''}</p></div>)}</div></section><section className="card below"><h2>コメント</h2><p className="muted">問題について気づいた点を残します。改善の進捗管理は行いません。</p><textarea aria-label="問題コメント" value={draft} onChange={e=>setDraft(e.target.value)} placeholder="問題文の条件を明確にする、など"/><div className="between"><span className="muted">このページ内で保持されます</span><button className="primary" onClick={()=>{setNotes({...notes,[key(item.id)]:draft});tell('コメントを保存しました')}}><Check size={16}/>コメントを保存</button></div></section></>:tab==='data'?<><div className="sectionhead"><div><h2>データ・資料</h2><p className="muted">{subject}のデータを登録・洗い替えします。</p></div><span className="badge">デモ版 {rev+1}</span></div><div className="callout">ファイル内容は送信・解析されません。ファイル名の反映と洗い替えの操作を評価できます。登録後も分析値は架空データです。</div>{['正誤CSV','設問分類CSV','問題PDF','正解・模範解答PDF','答案PDF（任意）'].map((kind,i)=><div className="card filerow" key={kind}><FileText size={25}/><div className="fileinfo"><h3>{kind}</h3><p className="muted">{files[context+'/'+kind]||(test?.demo?`${subject}_${['正誤.csv','分類と配点.csv','問題.pdf','模範解答.pdf','答案6人分.pdf'][i]}`:'未登録')}</p><small>{['生徒ID・学校ID・クラス番号・指導者名・正誤（空欄は未回答）','大問・小問・枝問・領域・単元・学習項目・配点','問題用紙のOCRと設問対応','正解と模範解答','生徒IDに対応づけた答案。氏名を含めないでください。'][i]}</small></div><span className="badge">{test?.demo||files[context+'/'+kind]?'登録済みの例':'未登録'}</span><label className="secondary upload"><Upload size={16}/>{ready?'差し替える':'登録する'}<input type="file" accept={kind.includes('CSV')?'.csv':'.pdf'} onChange={e=>{const f=e.target.files?.[0];if(f){setPending({kind,name:f.name});setModal('upload')}e.target.value=''}}/></label></div>)}<div className="actions below"><button className="secondary" onClick={()=>{setTab('items');openItem(20);setDraft(stems[subject][2]);setModal('ocr')}}>OCR確認の例を見る</button><button className="primary" disabled={!ready} onClick={()=>{navigate('summary');tell('サンプル分析を表示しました（実計算は行っていません）')}}>サンプル分析を見る<ArrowRight size={16}/></button></div></>:null}</>}
<footer className="pagefoot">Testscope <span>問題と問題セットの品質を、次の出題へ。</span></footer></main>
<Sheet open={ai} onOpenChange={setAi}><SheetContent className="aipanel"><div className="aihead"><span className="aisymbol"><Sparkles/></span><SheetTitle>AIと改善を考える</SheetTitle><SheetDescription>{test?.name} / {subject}{item?` / 設問 ${item.no}`:' / 問題セット'}</SheetDescription></div><div className="aibody"><div className="callout">AI回答の操作デモです。定型回答を表示し、外部AIには送信しません。</div><h3>どこから検討しますか？</h3>{(item?['この問題の改善案を教えて','このグラフから何が分かる？','実際の誤答から気になる点は？']:['問題セットの良い点と問題点は？','出題内容や難易度の偏りを説明して','構成の改善案を教えて']).map(q=><button className="prompt" key={q} onClick={()=>ask(q)}>{q}<ArrowUpRight size={15}/></button>)}{reply&&<><div className="userbubble">{asked}</div><div className="aireply"><b><Sparkles size={16}/> フィードバック例</b><p>{reply}</p><button className="textlink" onClick={()=>{setAi(false);if(item)openItem(item.id);else{navigate('items');setFilter('要確認')}}}>根拠の問題を確認 <ArrowRight size={15}/></button>{item&&<button className="secondary full" onClick={()=>{setDraft((notes[key(item.id)]||'')+'\nAI改善案（デモ）\n'+reply);setAi(false);tell('コメント欄へ引用しました。確認して保存してください')}}>コメントに引用する</button>}</div></>}</div><form className="aiform" onSubmit={e=>{e.preventDefault();ask(question)}}><label className="sr-only" htmlFor="question">AIへの質問</label><input id="question" value={question} onChange={e=>setQuestion(e.target.value)} placeholder="改善したい点を入力…"/><button className="primary" disabled={!question.trim()} aria-label="質問を送信"><Send size={18}/></button></form></SheetContent></Sheet>
<Dialog open={!!modal} onOpenChange={v=>{if(!v){setModal('');if(item)setDraft(notes[key(item.id)]||'')}}}><DialogContent className={modal==='keys'?'wide-dialog':'edit-dialog'}><DialogTitle>{modal==='series'?'シリーズを登録':modal==='test'?'テストを登録':modal==='period'?'テスト名・実施期間':modal==='keys'?'共通問題・IRTキー':modal==='ocr'?'OCRテキストを確認': 'データの洗い替え'}</DialogTitle><DialogDescription>{modal==='keys'?'同一シリーズ・同一教科の過去回と比較します。候補検出はデモです。':modal==='upload'?'このテスト・教科の対象データ種別を全件置換します。':'入力内容はこのページ内で保持されます。'}</DialogDescription>
{['series','test','period'].includes(modal)?<form onSubmit={e=>{e.preventDefault();if(modal!=='series'&&start>end){tell('終了日は開始日以降にしてください');return}if(modal==='series'){const id='s'+Date.now();setSeries([...series,{id,name:name.trim(),tests:[]}]);setSid(id);setView('tests')}else if(modal==='test'){const id='t'+Date.now();setSeries(series.map(s=>s.id===sid?{...s,tests:[...s.tests,{id,name:name.trim(),start,end,demo:false}]}:s));setTid(id);setView('test');navigate('data')}else setSeries(series.map(s=>s.id===sid?{...s,tests:s.tests.map(t=>t.id===tid?{...t,name:name.trim(),start,end}:t)}:s));setModal('');tell('登録内容を反映しました')}}><label className="field">{modal==='series'?'シリーズ名':'テスト名'}<input required value={name} onChange={e=>setName(e.target.value)} placeholder="中学3年生向け模擬試験"/></label>{modal!=='series'&&<div className="grid2"><label className="field">実施開始日<input required type="date" value={start} onChange={e=>setStart(e.target.value)}/></label><label className="field">実施終了日<input required type="date" value={end} min={start} onChange={e=>setEnd(e.target.value)}/></label></div>}<button className="primary full below" disabled={!name.trim()}>保存する</button></form>:modal==='upload'&&pending?<><div className="callout"><b>{subject} / {pending.kind}</b><p>新しいファイル：{pending.name}</p><p>集計・IRT・AI説明は更新対象になります。</p></div><p>このモックでは内容の検証・OCR・実分析は行わず、ファイル名とデモ版を更新します。</p><button className="primary" onClick={()=>{setFiles({...files,[context+'/'+pending.kind]:pending.name});setRevision({...revision,[context]:rev+1});setReply('');setAsked('');setModal('');tell('洗い替えの操作を反映しました。分析値はサンプルのままです')}}>洗い替えを試す</button></>:modal==='ocr'?<><textarea aria-label="OCR抽出文" value={draft} onChange={e=>setDraft(e.target.value)}/><p className="muted">OCR抽出文の編集デモです。実PDFは処理しません。</p><button className="primary" onClick={()=>{if(item)setOcr({...ocr,[key(item.id)]:draft});setModal('');if(item)setDraft(notes[key(item.id)]||'');tell('OCR確認内容を反映しました')}}>確認して保存</button></>:modal==='keys'?<><p className="muted">第1回 ↔ 第2回 / {subject} · 比較はデモです</p>{[0,7,13].filter(i=>!excluded[key(i)]).map(i=><div className="keyrow" key={i}><div className="grid2"><div className="paper"><b>第1回 / 設問 {items[i].no}</b><p>{items[i].stem}</p></div><div className="paper"><b>第2回 / 設問 {items[i].no}</b><p>{items[i].stem}{i===7?'（説明を追加）':''}</p></div></div><div className="between below"><span className="badge">{i===7?'類似候補・文言差あり':'同一候補'}</span><div className="actions"><button className="textlink" onClick={()=>setExcluded({...excluded,[key(i)]:true})}>候補から除外</button><label className="mark">IRTキー<Switch aria-label={`設問${items[i].no}をキーに設定`} checked={!!keys[key(i)]} onCheckedChange={v=>{setKeys({...keys,[key(i)]:v});tell('キー設定を反映しました（再分析はデモ）')}}/></label></div></div></div>)}<p className="footnote">類似だけでは共通項目として確定しません。実製品では適格性を確認して再分析します。</p></>:null}</DialogContent></Dialog>{notice&&<div className="notification" role="status"><CheckCircle2 size={18}/>{notice}</div>}</>}
